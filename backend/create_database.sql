-- Create the database (run this in MySQL if the database doesn't exist)
CREATE DATABASE IF NOT EXISTS spring;
USE spring;

-- The tables will be created automatically by Hibernate
-- But you can run this manually if needed